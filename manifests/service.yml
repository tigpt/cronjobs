apiVersion: v1
kind: Service
metadata:
    name: "cluster1"
    labels:
        app: "cluster1"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "cluster1"